import 'package:flutter/material.dart';

class DaySheetScreen extends StatelessWidget {
  const DaySheetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Day Sheet"),
      ),
    );
  }
}