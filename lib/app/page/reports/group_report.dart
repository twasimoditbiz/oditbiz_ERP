import 'package:flutter/material.dart';

class GroupReportScreen extends StatelessWidget {
  const GroupReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Group  Report"),
      ),
    );
  }
}